# 🔴 重要：需要下载 FFmpeg

## 当前状态
❌ **FFmpeg 尚未下载**，用户暂时**无法使用软件自带的 FFmpeg**。

## 📥 立即下载

### 选项 1：自动脚本（最简单）

在项目根目录打开 PowerShell，运行：

```powershell
.\download_ffmpeg.ps1
```

**预计时间**：2-5 分钟（取决于网速）

---

### 选项 2：手动下载

1. **访问下载页面**：
   https://github.com/BtbN/FFmpeg-Builds/releases

2. **下载文件**：
   `ffmpeg-master-latest-win64-gpl.zip` （约 100MB）

3. **解压并提取**：
   - 解压 ZIP 文件
   - 找到 `bin/ffmpeg.exe`

4. **复制到项目**：
   ```
   复制 ffmpeg.exe 到：
   d:\dov\load\xinghe\windows\ffmpeg\ffmpeg.exe
   ```

---

## ✅ 下载完成后

### 验证文件
```powershell
ls windows\ffmpeg\ffmpeg.exe

# 应该显示：
# 文件大小约 100-120 MB
```

### 构建应用
```bash
flutter build windows --release
```

### 验证打包
```powershell
ls build\windows\x64\runner\Release\ffmpeg.exe

# 应该自动复制到这里 ✅
```

---

## 🎯 完成后的效果

### ✅ 用户体验
- 用户下载您的应用后，**开箱即用**
- **无需安装** FFmpeg
- **无需配置** PATH 环境变量

### ✅ 发布包
```
xinghe-v1.0.0-windows.zip
├── xinghe.exe
├── ffmpeg.exe  ← 自动包含
├── flutter_windows.dll
└── data/
```

---

## ⚠️ 重要提示

**下载 FFmpeg 是一次性操作**：
1. 开发者下载一次
2. 构建时自动打包
3. 用户无需任何操作

**Git 协作**：
- FFmpeg 已添加到 `.gitignore`（文件太大）
- 团队成员需要各自运行 `download_ffmpeg.ps1`

---

## 📝 后续步骤

1. ✅ 下载 FFmpeg
2. ✅ 运行 `flutter build windows --release`
3. ✅ 测试上传角色功能
4. ✅ 打包发布

**现在就运行 `.\download_ffmpeg.ps1` 来完成最后一步！** 🚀
